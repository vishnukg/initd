# Closer matches for `cosmos-dark`

This set is filtered against the three references you selected:

- `01-1515` and `02-1520`: isolated blue planets with large black negative space.
- `05-15601`: sparse blue/green star fields on a mostly black background.

The first eight files are planet compositions; `09-10307` and `10-11097` are the
sparser star-field options. Originals are kept at their downloaded resolution.

[Open the preview sheet](preview.jpg)

Exact source and download URLs are in `sources.json`.
