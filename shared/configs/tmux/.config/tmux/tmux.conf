# Prefix: Ctrl+a
#
# Keep tmux in one explicit key namespace:
#   C-a <key> = tmux
#   \ <key>   = Neovim leader
#   C-w <key> = Neovim windows/splits
#
# If a program needs a real Ctrl+a, press C-a C-a.
unbind C-b
unbind C-Space
set -g prefix C-a
bind C-a send-prefix

# Windows
bind c new-window -c "#{pane_current_path}"
bind Tab next-window
bind BTab previous-window
bind n next-window
bind p previous-window
bind , command-prompt -I "#W" "rename-window '%%'"

# Sessions
bind s choose-tree -s

# Splits (open in current path)
bind b  split-window -v -c "#{pane_current_path}"
bind v split-window -h -c "#{pane_current_path}"

# Pane navigation. Use the prefix so these never steal keys from Neovim.
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

# Pane resizing. Arrow keys with repeat so prefix only needed once.
bind -r Left  resize-pane -L 5
bind -r Down  resize-pane -D 5
bind -r Up    resize-pane -U 5
bind -r Right resize-pane -R 5

# Handy pane/window management.
bind z resize-pane -Z
bind x confirm-before -p "kill-pane #P? (y/n)" kill-pane
bind X confirm-before -p "kill-window #W? (y/n)" kill-window
bind r source-file ~/.config/tmux/tmux.conf \; display-message "tmux config reloaded"

# Start windows and panes at 1
set -g base-index 1
set -g pane-base-index 1
set -g renumber-windows on

set -sg escape-time 0
set -g history-limit 10000
setw -g mode-keys vi
set -g mouse on
set -g focus-events on
set -g repeat-time 1000

# True color
set -g default-terminal "tmux-256color"
set -ga terminal-overrides ",xterm-256color:Tc"
set -ga terminal-overrides ",xterm-kitty:Tc"
set -ga terminal-overrides ",tmux-256color:Tc"
set -as terminal-features "xterm-ghostty:RGB"
set-environment -g COLORTERM truecolor

# --- VSCode Dark — Pastel Green accent (matches Kitty) ---
# Active green: #4ec994   Inactive bg: #24283b
# Inactive text: #7aa2f7  Active text: #141414

# Pane borders
set -g pane-border-style "fg=#24283b"
set -g pane-active-border-style "fg=#4ec994"
set -g display-panes-colour "#727169"
set -g display-panes-active-colour "#4ec994"

set -g status on
set -g status-interval 5
set -g status-position bottom
set -g status-justify left
set -g status-style "bg=default,fg=#dcd7ba"

set -g status-left-length 40
set -g status-left "#[fg=#4ec994,bg=default]#[bg=#4ec994,fg=#141414,bold] #S #[bg=default,fg=#4ec994] "

set -g automatic-rename on
set -g automatic-rename-format "#{pane_current_command}"

set -g window-status-separator " "
set -g window-status-format        "#[fg=#24283b,bg=default]#[fg=#7aa2f7,bg=#24283b] #I:#{window_name}:#{b:pane_current_path}:#{?#{==:#{window_index},1},🚀,#{?#{==:#{window_index},2},🔥,#{?#{==:#{window_index},3},⚡,#{?#{==:#{window_index},4},🌈,#{?#{==:#{window_index},5},🦄,#{?#{==:#{window_index},6},💎,#{?#{==:#{window_index},7},🎯,#{?#{==:#{window_index},8},🌊,#{?#{==:#{window_index},9},🦋,#{?#{==:#{window_index},10},🌺,#{?#{==:#{window_index},11},🍄,#{?#{==:#{window_index},12},🎸,#{?#{==:#{window_index},13},🌙,#{?#{==:#{window_index},14},⭐,#{?#{==:#{window_index},15},🎨,#{?#{==:#{window_index},16},🪄,🪄}}}}}}}}}}}}}}}} #[fg=#24283b,bg=default]"
set -g window-status-current-format "#[fg=#4ec994,bg=default]#[fg=#141414,bg=#4ec994,bold] #I:#{window_name}:#{b:pane_current_path}:#{?#{==:#{window_index},1},🚀,#{?#{==:#{window_index},2},🔥,#{?#{==:#{window_index},3},⚡,#{?#{==:#{window_index},4},🌈,#{?#{==:#{window_index},5},🦄,#{?#{==:#{window_index},6},💎,#{?#{==:#{window_index},7},🎯,#{?#{==:#{window_index},8},🌊,#{?#{==:#{window_index},9},🦋,#{?#{==:#{window_index},10},🌺,#{?#{==:#{window_index},11},🍄,#{?#{==:#{window_index},12},🎸,#{?#{==:#{window_index},13},🌙,#{?#{==:#{window_index},14},⭐,#{?#{==:#{window_index},15},🎨,#{?#{==:#{window_index},16},🪄,🪄}}}}}}}}}}}}}}}} #[fg=#4ec994,bg=default,nobold]"

set -g status-right-length 55
set -g status-right "#[fg=#24283b,bg=default]#[fg=#7aa2f7,bg=#24283b,bold] #(pmset -g batt | grep -o '[0-9]*%%' | head -1)  %d %b  %H:%M #[fg=#24283b,bg=default]"

set -g message-style "bg=#24283b,fg=#dcd7ba"
set -g message-command-style "bg=#24283b,fg=#4ec994"
set -g mode-style "bg=#4ec994,fg=#141414"

set -g clock-mode-colour "#4ec994"
set -g clock-mode-style 24

# Mouse scroll — pass to pane if it handles mouse (nvim, fzf), else enter copy-mode
bind -n WheelUpPane   if-shell -F "#{mouse_any_flag}" "send-keys -M" "if -Ft= '#{pane_in_mode}' 'send-keys -M' 'copy-mode -e'"
bind -n WheelDownPane if-shell -F "#{mouse_any_flag}" "send-keys -M" "if -Ft= '#{pane_in_mode}' 'send-keys -M' ''"

# Copy to system clipboard
bind -T copy-mode-vi y     send -X copy-pipe-and-cancel 'pbcopy'
bind -T copy-mode-vi Escape send -X cancel
