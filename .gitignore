# Local Go build output.
bin/

# Claude Code local settings (machine-specific).
.claude/

# Machine-local git identity (email) — never commit this.
shared/configs/git/local.gitconfig

# Licensed fonts (Berkeley Mono). This repo is public: committing the files
# would redistribute paid fonts. The directory is a clone of the PRIVATE repo
# github.com/vishnukg/fonts, synced by shared/lib/fonts.sh during bootstrap;
# platform steps then install from it (macOS copies, Linux links). Absent on
# machines without access — everything downstream skips gracefully.
shared/fonts/

# Fish — files managed by fisher or fish itself, not by this repo.
shared/configs/fish/.config/fish/fish_variables
shared/configs/fish/.config/fish/local.fish
shared/configs/fish/.config/fish/conf.d/local.fish
shared/configs/fish/.config/fish/functions/
shared/configs/fish/.config/fish/completions/
shared/configs/fish/.config/fish/conf.d/*
!shared/configs/fish/.config/fish/conf.d/00-initd-env.fish

# Terminals — the SYMLINK only. Ghostty has no per-OS include, so
# linux/setup.sh:link_ghostty_linux_conf drops this link next to the shared
# config, pointing at the tracked linux/configs/ghostty/linux.conf. The content
# is committed (it is a Linux value, not a per-machine one); only the generated
# link is ignored. kitty needs no equivalent — it expands ${KITTY_OS}.
shared/configs/ghostty/.config/ghostty/linux.conf

# Neovim — generated plugin directory.
shared/configs/nvim/.config/nvim/plugin/

# ruby-lsp drops this in whatever directory nvim is opened from.
.ruby-lsp/

# GTK — bookmarks are written at runtime by file picker dialogs.
linux/configs/gtk-3.0/bookmarks

# hyprmoncfg generates the active monitor rules from the tracked profiles.
linux/configs/hypr/hyprmoncfg-monitors.lua
