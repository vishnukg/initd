# Local Go build output.
bin/

# Claude Code local settings (machine-specific).
.claude/

# Machine-local git identity (email) — never commit this.
git/local.gitconfig

# Fish — machine-local files managed by fish itself.
fish/.config/fish/fish_variables
fish/.config/fish/conf.d/local.fish

# Neovim — generated plugin directory.
nvim/.config/nvim/plugin/
