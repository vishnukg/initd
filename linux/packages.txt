# apt packages installed by linux/bootstrap.sh.
# One package per line, comments start with #.
# Targets Ubuntu 26.04 (resolute) — every package below is in the official
# archive (main/universe), no PPAs or source builds required.

# ── Hyprland desktop (Wayland; installed alongside stock GNOME) ──────────────
# The hyprland package ships /usr/share/wayland-sessions/hyprland.desktop, so
# GDM offers Hyprland as a session next to Ubuntu's default GNOME.
hyprland
hyprlock
hypridle
hyprpaper
hyprland-qtutils
xdg-desktop-portal-hyprland
xdg-desktop-portal-gtk
xwayland
waybar
rofi
dunst
libnotify-bin
policykit-1-gnome
nwg-displays
grim
slurp
wl-clipboard
gammastep
gnome-software

# ── System utilities ──────────────────────────────────────────────────────────
# VA-API driver for Intel iGPU video decode (Firefox media.ffmpeg.vaapi.enabled
# silently falls back to CPU decoding without it; non-free variant adds HEVC).
intel-media-va-driver-non-free
pulseaudio-utils
brightnessctl
ca-certificates
curl
unzip
network-manager-gnome
blueman
copyq
power-profiles-daemon

# ── Theming ───────────────────────────────────────────────────────────────────
arc-theme
papirus-icon-theme
dmz-cursor-theme
# Firefox and the desktop use these proportional/serif families explicitly;
# list them even though a full Ubuntu desktop normally pulls them in already.
fonts-ubuntu
fonts-noto-core
fonts-firacode

# ── Dev tools (cross-platform equivalents of Brewfile entries) ───────────────
fish
tmux
tig
git
gnupg

# ── Build dependencies for mise-managed Python / Node ────────────────────────
build-essential
autoconf
libssl-dev
libreadline-dev
libyaml-dev
libgmp-dev
zlib1g-dev
libffi-dev
libxml2-dev
libxslt1-dev
