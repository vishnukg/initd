# hyprpaper — replaces nitrogen. Wallpaper is machine-local (not in the repo):
# drop an image at ~/Pictures/wallpaper.png and it applies to all monitors.
# hyprland.conf only starts hyprpaper when that file exists.

preload = ~/Pictures/wallpaper.png
wallpaper = , ~/Pictures/wallpaper.png
splash = false
