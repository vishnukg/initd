# Written by nwg-displays (its default output path is ~/.config/hypr/monitors.conf,
# which is this file via the repo symlink). Monitor lines here are more specific
# than the catch-all rules in hyprland.conf, so they win for the named outputs.
# Arrange monitors in the nwg-displays GUI and hit Apply to populate this file.
