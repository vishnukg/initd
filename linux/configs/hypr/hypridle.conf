# hypridle — replaces xss-lock. Locks via hyprlock on loginctl lock-session
# and before suspend; turns displays off after 10 minutes idle.

general {
    # Guarded: hypridle fires lock_cmd on every lock-session event, and a
    # hyprlock that fails to exit on unlock would otherwise leave one ~105 MB
    # process behind per suspend cycle (four had accumulated over four days).
    # `pidof` makes the lock idempotent — reuse the live instance if there is one.
    lock_cmd = pidof hyprlock || hyprlock
    before_sleep_cmd = loginctl lock-session
    inhibit_sleep = 3
}

# Screen off after 10 min idle (no auto-lock — matches the old i3 setup,
# which only locked on suspend or an explicit $mod+Shift+x).
listener {
    timeout = 600
    on-timeout = hyprctl dispatch 'hl.dsp.dpms("off")'
    on-resume = hyprctl dispatch 'hl.dsp.dpms("on")'
}
