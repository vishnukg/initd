{
  "name": "initd",
  "private": true,
  "type": "module",
  "description": "Dev-only tooling. Nothing here is needed to run the scripts: Node strips the types at load time.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --test shared/install.test.ts shared/agent-status.test.ts shared/enterprise-quota.test.ts shared/fish-config.test.ts"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0"
  }
}
