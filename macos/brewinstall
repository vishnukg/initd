#!/usr/bin/env node
// Preserve the existing command name; implementation and tests use plain ESM.
import('./brewinstall.mjs').then(({ main }) => main(process.argv.slice(2))).catch(error => {
    console.error(`brewinstall: ${error.message}`);
    process.exitCode = 1;
});
